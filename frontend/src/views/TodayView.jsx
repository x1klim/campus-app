import styles from './TodayView.module.css';

const TodayView = () => {
  return (
    <div className={styles.container}>
      <span className={styles.placeholder}>Today</span>
    </div>
  );
};

export default TodayView;
